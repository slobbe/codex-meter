# Implementation Plans

Generated by the improve skill on 2026-07-02. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, run every verification command, and update your row when done.

Planned against commit `d5233e7` (`Fix native append byte writes`). The working branch was `main`, ahead of `origin/main` by 10 commits at planning time.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [001](001-cancellable-banked-reset-requests.md) | Make banked-reset requests cancellable during extension teardown | P1 | M | — | DONE |
| [002](002-sanitize-ui-error-details.md) | Stop showing raw technical error details in user-facing UI | P1 | S | — | DONE |
| [003](003-document-local-storage-privacy.md) | Document local usage, history, and banked-reset storage in the privacy section | P1 | S | — | TODO |
| [004](004-minimize-banked-reset-cache-metadata.md) | Minimize persisted banked-reset metadata | P2 | S | 003 recommended | TODO |
| [005](005-add-codex-api-auth-characterization-tests.md) | Add characterization tests for Codex API, auth, and banked-reset parsing | P2 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plan 001 can land independently and addresses the highest correctness risk: cancellation does not currently reach banked-reset GET/POST calls.
- Plan 002 can land independently and is intentionally small; it reduces user-visible privacy exposure from raw technical error details.
- Plan 003 can land independently and should ideally land before or with any privacy/storage behavior changes.
- Plan 004 does not strictly require Plan 003, but if Plan 003 lands first, Plan 004's executor must re-check README privacy wording after minimizing the cache shape.
- Plan 005 can land independently. It is a useful prerequisite before larger provider/cache/refactor work, but the top privacy/cancellation fixes should not wait on it if they are urgent.

## Verification baseline from recon

The advisor ran these read-only commands during audit:

- `npx tsc --noEmit` — passed.
- `npm audit --audit-level=high` — passed with `0 vulnerabilities`.

The advisor did not run `npm test` or `make clean pack` because those commands write build/bundle artifacts such as `dist` and zip files. Executors are expected to run the verification commands listed inside their specific plans.

Repo validation commands discovered:

| Purpose | Command | Notes |
|---|---|---|
| Typecheck without emit | `npx tsc --noEmit` | Safe preflight, passed during recon |
| Build | `npm run build` | Writes `dist` |
| Test suite | `npm test` | Runs build, Node tests, and `gjs -m test/history-storage.test.js` |
| CI extras | See `.github/workflows/ci.yml` | Schema validation, packaging, bundle checks, Shexli |

## Findings considered and rejected or deferred

- New `Soup.Session` per API call: real pattern in `src/infra/api_client.ts`, but user-visible impact is uncertain without measurement in target GNOME/GJS environments. Deferred as investigate-later rather than a top implementation plan.
- Broad UI god-module split: `src/ui/panel-indicator.ts`, `src/ui/popup-menu.ts`, and `src/ui/preferences-pages.ts` are large, but a broad refactor is lower leverage than first adding lifecycle/API characterization tests.
- Hardcoded secrets: not found in audited source/config.
- High/critical npm dependency vulnerabilities: `npm audit --audit-level=high` found `0 vulnerabilities`.
- Provider-abstraction roadmap: grounded direction option, but not selected for implementation plans in this batch.
- Banked-reset confirmation UX: grounded direction option, but not selected for implementation plans in this batch.
- User-facing privacy/storage controls: grounded direction option, but not selected for implementation plans in this batch.

## Audit scope notes

Audited categories at standard depth: correctness/bugs, security/privacy, performance, test coverage, tech debt/architecture, dependencies/migrations, DX/tooling, docs, and direction. The audit focused on `src/`, `test/`, root docs/config, and GitHub workflows. Generated/ignored artifacts such as `dist/`, `node_modules/`, and release zip bundles were not audited in depth.